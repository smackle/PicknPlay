<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kiit.pickandplay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name=".homeActivity"
            android:theme="@style/MyMaterialTheme.Base">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ChoicePick" />
        <activity android:name=".movieone" />
        <activity android:name=".movietwo" />
        <activity android:name=".threefragment" />
        <activity android:name=".MoviesActivity" />
        <activity android:name=".MovieSuggestion" />
        <activity android:name=".MovieList" />
        <activity android:name=".MusicActivities" />
        <activity android:name=".MusicSuggestion" />
        <activity android:name=".MusicList" />
        <activity android:name=".frch" />
    </application>

</manifest>